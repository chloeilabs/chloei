# TradingAgents sidecar service dependencies.
#
# The service wraps the upstream TauricResearch/TradingAgents framework and
# exposes it over HTTP/SSE for the Chloei frontend. TradingAgents itself pulls
# in langchain, langgraph, yfinance, etc. via its own project metadata.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0

# Upstream multi-agent framework, pinned to the commit this integration was
# verified against (v0.2.5). The service relies on version-specific behavior and
# internal APIs (e.g. graph._resolve_pending_entries), so an unpinned HEAD could
# silently break builds or change results on a future upstream change. To develop
# against a local checkout instead, comment this out and `pip install -e
# ../path/to/TradingAgents`.
tradingagents @ git+https://github.com/TauricResearch/TradingAgents.git@04f434e86db88e7707bf16db8ed7183f9764fe26
